<?php
$title = 'My Films';
$this->headTitle($title);
?>
<h1><?= $this->escapeHtml($title) ?></h1>
<p>
    <a href="<?= $this->url('film', ['action' => 'add']) ?>">Add new film</a>
</p>

<table class="table">
    <tr>
        <th>Synopsis</th>
        <th>Genre</th>
        <th>Date Sortie</th>
        <th>&nbsp;</th>
    </tr>
    <?php foreach ($this->films as $film) : ?>
        <?php /** @var \Film\Entity\Film $film */ ?>
        <tr>
            <td><?= $this->escapeHtml($film->getTitle()) ?></td>
            <td><?= $this->escapeHtml($film->getGenre()) ?></td>
            <td>
                <a href="<?= $this->url('album', ['action' => 'edit', 'id' => $film->getId()]) ?>">Edit</a>
                <a href="<?= $this->url('album', ['action' => 'delete', 'id' => $film->getId()]) ?>">Delete</a>
            </td>
        </tr>
    <?php endforeach; ?>
</table>
